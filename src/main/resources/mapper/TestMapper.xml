<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.dao.TestDao">

	<!-- 测试查询 -->
	<select id="test11"  resultType="java.util.HashMap" parameterType="java.lang.String">
		SELECT * FROM test WHERE reserve1 = #{str}
	</select>
	<!-- 测试获取用户id -->
	<select id="test22"  resultType="java.util.HashMap" parameterType="java.lang.String">
		SELECT user_id,user_name FROM user WHERE user_id = #{str}
	</select>

</mapper> 
